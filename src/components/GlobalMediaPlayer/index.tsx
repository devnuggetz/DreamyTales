import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const GlobalMediaPlayer = ({children}) => {
  return children;
};

export default GlobalMediaPlayer;

const styles = StyleSheet.create({});
